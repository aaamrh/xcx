<!--components/like/index.wxml-->
<view bind:tap="onLike" class="container"> <!-- bind-like 这里用到了自定义事件, like -->
  <image src="{{ like ? likePng : dislikePng }}" />
  <text>{{ count }}</text>
</view>
