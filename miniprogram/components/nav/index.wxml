<view class="container">
  <image class="icon" src="{{ !latest && leftSrc }}" bind:tap="onLeft" />
  <text class="title">{{title}}</text>
  <image class="icon" src="{{ !first && rightSrc }}" bind:tap="onRight"/>
</view>